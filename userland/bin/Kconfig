menu "Binaries"

config BUILD_INIT
    bool "Init"
    default y

config BUILD_SHELL
    bool "Shell"
    default y
    depends on BUILD_TTY

config BUILD_CAT
    bool "Cat"
    default y
    depends on BUILD_SHELL

endmenu
