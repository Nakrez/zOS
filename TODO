[MISC]

- Rename all glue specific data structure as XXX_glue_data

[CORE]

- Review CPU to be sure it could support SMP (which I doubt)
- Add IPC mechanism
- Review ELF loader (Move it to its own files & map memory with correct rights)
- Review locks (Some might be missing)
- !!! Check user pointers from syscalls !!!

[MEM]

- Change page_size argument by size (in byte) and compute page_size inside
  the function

[SYSCALL]

- Add memory specific syscalls (mmap, munmap)
- Add IPC syscalls

[SCHEDULER]

- Remove x86 asm inline in core scheduler

[THREAD]

- Add thread_join()
- TLS kernel ?

[X86]

- Add APIC support
- Add SMP support
- Add GPF handler

[USERLAND]

- Add init binary
